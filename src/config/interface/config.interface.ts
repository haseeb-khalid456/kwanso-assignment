export interface ConfigInterface {
  NODE_ENV: string;
  PORT: string;
  JWT_TOKEN: string;
  DB_HOST: string;
  DB_PORT: number;
  DB_USERNAME: string;
  DB_PASSWORD: string;
  DB_SCHEMA: string;
  DB_NAME: string;
}
